# Copy to .env.local and fill in. All NEXT_PUBLIC_* vars are inlined at build time.

# Supabase (kiosk uses the publishable/anon key; tts-web.ts has a demo fallback)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
